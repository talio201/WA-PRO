### Backend server
PORT=3000
# Optional. Used mainly for upload URLs when behind reverse proxies.
# BASE_URL=http://localhost:3000

MONGO_URI=mongodb://localhost:27017/whatsapp_campaign_manager

### Storage provider
# local | supabase
STORAGE_PROVIDER=supabase
# You can also force-enable Supabase via SUPABASE_ENABLED=true
SUPABASE_ENABLED=true

### Supabase configuration (required when STORAGE_PROVIDER=supabase)
SUPABASE_URL=
SUPABASE_ANON_KEY=
# Recommended for backend writes (bypasses RLS safely when kept server-side).
# Keep this secret and never commit it.
SUPABASE_SERVICE_ROLE_KEY=

### Google Gemini AI
GEMINI_API_KEY=
GEMINI_REQUEST_TIMEOUT_MS=20000

### Optional realtime webhooks (comma-separated URLs)
WEBHOOK_TARGETS=
# Optional signature secret for webhook validation (HMAC SHA-256 in X-WA-Signature).
# Keep this secret and never commit it.
WEBHOOK_SECRET=
