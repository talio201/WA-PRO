{
  "name": "emidia-saas-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix webapp",
    "dev:backend": "npm --prefix backend run dev",
    "dev:webapp": "npm --prefix webapp run dev",
    "dev:saas": "concurrently \"npm run dev:backend\" \"npm run dev:webapp\"",
    "clean": "npm --prefix backend run clean && npm --prefix webapp run clean"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
