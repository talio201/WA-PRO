version: '3.8'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - STORAGE_PROVIDER=local
      - API_SECRET_KEY=[REDACTED_API_SECRET]
    volumes:
      - ./backend/uploads:/app/uploads
      - ./backend/data:/app/data
    restart: always

  python_bot:
    build:
      context: ./python_bot
    environment:
      - API_SECRET_KEY=[REDACTED_API_SECRET]
      - API_BASE_URL=http://backend:3000/api
    # O bot precisa acessar a rede do host ou do serviço backend.
    # Usaremos a rede interna do docker-compose onde o host 'backend' resolve para o contêiner Node.js.
    depends_on:
      - backend
    volumes:
      - ./python_bot/whatsapp_session:/app/whatsapp_session
    restart: always
