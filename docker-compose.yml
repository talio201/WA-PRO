services:
  redis:
    image: redis:7-alpine
    command: ["redis-server", "--appendonly", "yes"]
    ports:
      - "127.0.0.1:6379:6379"
    volumes:
      - redis_data:/data
    restart: always

  backend:
    build:
      context: ./backend
    ports:
      - "127.0.0.1:3000:3000"
    env_file:
      - ./backend/.env
    environment:
      - PORT=3000
      - STORAGE_PROVIDER=supabase
      - REDIS_URL=redis://redis:6379
    volumes:
      - ./backend/uploads:/app/uploads
      - ./backend/data:/app/data
    depends_on:
      - redis
    restart: always

  backend_worker:
    build:
      context: ./backend
    env_file:
      - ./backend/.env
    environment:
      - REDIS_URL=redis://redis:6379
      - STORAGE_PROVIDER=supabase
    command: ["npm", "run", "worker:helpdesk"]
    volumes:
      - ./backend/data:/app/data
    depends_on:
      - backend
      - redis
    restart: always

  python_bot:
    build:
      context: ./python_bot
    env_file:
      - ./backend/.env
    environment:
      - API_BASE_URL=http://backend:3000/api
      - API_SECRET_KEY=${API_SECRET_KEY:-emidia-session-secret}
      - BOT_API_KEY=${BOT_API_KEY:-emidia-session-secret}
      - BOT_AGENT_ID=${BOT_AGENT_ID:-bot}
    # O bot precisa acessar a rede do host ou do serviço backend.
    # Usaremos a rede interna do docker-compose onde o host 'backend' resolve para o contêiner Node.js.
    depends_on:
      - backend
      - backend_worker
      - redis
    volumes:
      - ./python_bot/whatsapp_session:/app/whatsapp_session
    restart: always

volumes:
  redis_data:
